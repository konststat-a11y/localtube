# Product Spec: локальный видеосайт

## 1. Назначение

Проект представляет собой локальный видеосайт, похожий по базовой логике на YouTube. Сайт запускается на компьютере пользователя, хранит видеофайлы в локальной папке и показывает приватные видео только тем пользователям, которым выдан доступ.

Текущий MVP объединяет ручное администрирование локальной видеотеки и пользовательские сценарии просмотра: регистрацию, вход, загрузку видео, профили, историю просмотров, реакции, комментарии, сохранение прогресса и базовую навигацию по доступным видео. Видео могут добавляться вручную в `video_storage/` через сканирование администратором или загружаться через сайт авторизованным пользователем.

## 2. Цели MVP

- Пользователь входит на сайт по логину и паролю.
- Пользователь может зарегистрировать обычный аккаунт.
- После входа пользователь видит только назначенные ему видео.
- Администратор создает пользователей вручную.
- Администратор сканирует папку с видео.
- Администратор редактирует метаданные видео.
- Администратор назначает каждому пользователю индивидуальный список доступных видео.
- Авторизованный пользователь может загрузить видео через сайт.
- Пользователь смотрит видео через HTML5-плеер с локальными элементами управления.
- Видео до 10 ГБ воспроизводятся без загрузки файла целиком в память.
- Поддерживается сортировка по названию и дате.
- Для каждого пользователя сохраняются история просмотров, список `Смотреть позже`, реакции, комментарии и прогресс просмотра.
- Пользователь может открыть свой профиль, чужой профиль и настройки аккаунта.
- Главная страница поддерживает светлую и темную тему, боковое меню и карточки с thumbnail/preview.

## 3. Не входит в MVP

- Подписки.
- Автоматическое определение длительности видео.
- Группы доступа и роли, кроме флага администратора.
- Публичные видео без авторизации.
- Поиск по названию и описанию.
- Категории видео и фильтрация по категориям.
- Выбор качества видео.
- Субтитры.
- Плейлисты.
- Уведомления.
- Жалобы и отдельная модераторская панель.
- Персональная рекомендательная система.
- Отдельный JSON API для будущего frontend-приложения.
- PostgreSQL вместо SQLite.

## 4. Пользователи и роли

### Обычный пользователь

Может:

- войти на сайт;
- зарегистрировать обычный аккаунт;
- выйти из аккаунта;
- видеть список доступных ему видео;
- сортировать видео по названию и дате;
- открыть страницу просмотра доступного видео;
- смотреть видео через браузерный плеер;
- загружать видео через сайт;
- открывать свой профиль и профили других пользователей;
- менять отображаемое имя, email, аватар, описание профиля и пароль;
- ставить лайк или дизлайк доступному видео;
- добавлять и удалять свои комментарии;
- добавлять видео в `Смотреть позже`;
- очищать историю просмотров;
- продолжать просмотр видео с сохраненной позиции.

Не может:

- видеть чужие видео;
- открывать чужое видео по прямой ссылке;
- получать поток чужого видео через stream-маршрут;
- заходить в админ-панель.

### Администратор

Может:

- выполнять все действия обычного пользователя;
- открывать админ-панель;
- создавать пользователей;
- создавать пользователей с флагом администратора;
- запускать сканирование папки `video_storage/`;
- видеть все видео в базе;
- редактировать название, описание и автора видео;
- назначать пользователям доступ к видео;
- модерировать комментарии;
- удалять любое видео.

## 5. Авторизация

Главная страница доступна гостю, но приватные действия и просмотр видео требуют авторизации. Неавторизованный пользователь может открыть форму входа или страницу регистрации, но не может открыть `/watch/{video_id}` и `/videos/{video_id}/stream`.

Основные маршруты:

```http
GET  /login
POST /login
GET  /register
POST /register
POST /logout
GET  /
GET  /watch/{video_id}
GET  /videos/{video_id}/stream
GET  /profile
GET  /account
GET  /users/{username}
```

Требования:

- пароль проверяется только по хешу;
- пароль нельзя хранить в открытом виде;
- после успешного входа создается cookie-сессия;
- после успешной регистрации создается cookie-сессия;
- новый зарегистрированный пользователь создается как обычный пользователь без прав администратора;
- неавторизованный пользователь при попытке открыть защищенную страницу перенаправляется на `/login`;
- stream-маршрут должен возвращать ошибку авторизации без HTML-редиректа, если это нужно для корректного поведения видеоплеера.

## 6. Главная страница

Маршрут:

```http
GET /
```

Главная страница должна содержать:

- верхнюю панель;
- название сайта;
- меню аккаунта с аватаром пользователя;
- ссылку в админ-панель для администратора;
- пункт `Головна` в боковом меню;
- разделы `История просмотров`, `Смотреть позже`, `Понравившиеся видео`;
- список или сетку доступных видео;
- сортировку по названию;
- сортировку по дате;
- переключение светлой и темной темы;
- сворачиваемое боковое меню.

Карточка видео должна показывать:

- название;
- автора;
- аватар автора или fallback-инициал;
- дату добавления;
- размер файла;
- thumbnail-превью или fallback-карточку;
- hover-preview видео;
- прогресс просмотра при наведении, если пользователь уже смотрел видео;
- признак недоступности файла, если файл пропал из папки хранения и это отображается в админке.

Обычный пользователь видит только видео, назначенные ему через `VideoAccess`.

## 7. Страница просмотра

Маршрут:

```http
GET /watch/{video_id}
```

Страница просмотра должна содержать:

- HTML5-видеоплеер с пользовательской панелью управления;
- название видео;
- описание;
- автора;
- дату добавления;
- icon-кнопки лайка и дизлайка со счетчиками;
- icon-кнопку `Смотреть позже`;
- кнопку `Поделиться`;
- комментарии;
- другие доступные видео;
- возможность удаления видео для администратора или автора.

Видеоплеер использует HTML5 `<video>` и stream endpoint:

```html
<video>
  <source src="/videos/{video_id}/stream">
</video>
```

Должны работать:

- запуск;
- пауза;
- перемотка;
- управление громкостью;
- полноэкранный режим;
- изменение скорости воспроизведения;
- режим кинотеатра;
- автопереход к следующему видео;
- сохранение и восстановление прогресса просмотра.

## 8. Админ-панель

Админ-панель доступна только пользователям с `is_admin = true`.

Основные маршруты:

```http
GET  /admin
GET  /admin/users
POST /admin/users/create
GET  /admin/videos
POST /admin/videos/scan
POST /admin/videos/{video_id}/update
GET  /admin/access
POST /admin/access/update
```

### Управление пользователями

Администратор должен иметь возможность:

- видеть список пользователей;
- создать пользователя;
- задать логин;
- задать пароль;
- указать, является ли пользователь администратором.

### Управление видео

Администратор должен иметь возможность:

- видеть список видео из базы;
- запустить сканирование папки `video_storage/`;
- видеть имя файла;
- видеть размер файла;
- видеть доступность файла;
- редактировать название;
- редактировать описание;
- редактировать автора;

### Управление доступом

Администратор должен иметь возможность:

- выбрать пользователя;
- увидеть список всех видео;
- отметить видео, доступные этому пользователю;
- сохранить индивидуальный список доступа.

Доступ не групповой. У каждого пользователя свой список видео.

## 9. Хранение видео

Видео физически хранятся в папке:

```text
video_storage/
```

Поддерживаемые форматы MVP:

- `.mp4`;
- `.m4v`;
- `.mov`;
- `.webm`;
- `.mkv`, если браузер может воспроизвести файл.

Сканирование должно:

- создать папку `video_storage/`, если ее нет;
- найти поддерживаемые видеофайлы;
- добавить новые видео в базу;
- обновить размер существующих видео;
- пометить отсутствующие файлы как недоступные;
- не удалять записи из базы автоматически.

Пользовательская загрузка должна:

- принимать файлы через `POST /videos/upload`;
- сохранять файл в `video_storage/` чанками;
- не читать весь файл в память;
- ограничивать размер загрузки до `10 ГБ`;
- создавать запись `Video`;
- считать автором текущего пользователя;
- выдавать доступ к загруженному видео обычным пользователям через `VideoAccess`;
- перенаправлять автора на страницу просмотра после успешной загрузки.

## 10. База данных

MVP использует SQLite.

Основные модели:

### User

- `id`;
- `username`;
- `email`;
- `password_hash`;
- `is_admin`;
- `created_at`.

### UserProfile

- `id`;
- `user_id`;
- `display_name`;
- `bio`;
- `avatar_path`;
- `created_at`;
- `updated_at`.

### Video

- `id`;
- `filename`;
- `title`;
- `description`;
- `author`;
- `category`;
- `file_path`;
- `size_bytes`;
- `created_at`;
- `updated_at`;
- `is_available`.

### VideoAccess

- `id`;
- `user_id`;
- `video_id`.

### ViewHistory

- `id`;
- `user_id`;
- `video_id`;
- `viewed_at`.

### VideoProgress

- `id`;
- `user_id`;
- `video_id`;
- `current_seconds`;
- `duration_seconds`;
- `updated_at`.

### WatchLater

- `id`;
- `user_id`;
- `video_id`;
- `created_at`.

### VideoReaction

- `id`;
- `user_id`;
- `video_id`;
- `value`;
- `created_at`;
- `updated_at`.

### Comment

- `id`;
- `user_id`;
- `video_id`;
- `body`;
- `created_at`.

Изменения структуры БД требуют отдельного согласования.

## 11. Streaming больших файлов

Маршрут:

```http
GET /videos/{video_id}/stream
```

Требования:

- проверять авторизацию;
- проверять доступ пользователя к видео;
- проверять существование файла;
- поддерживать HTTP-заголовок `Range`;
- отдавать частичный контент со статусом `206 Partial Content`;
- возвращать `Accept-Ranges`;
- возвращать `Content-Range` для частичных ответов;
- возвращать корректный `Content-Length`;
- определять `Content-Type` по файлу;
- читать файл кусками;
- не загружать файл целиком в память.

Ошибки:

- `401 Unauthorized` - пользователь не вошел;
- `403 Forbidden` - нет доступа к видео;
- `404 Not Found` - видео или файл не найден;
- `416 Range Not Satisfiable` - некорректный диапазон байтов.

## 12. Сортировка

Главная страница поддерживает параметры:

```http
/?sort=title&order=asc
/?sort=date&order=desc
```

Сортировка:

- по названию;
- по дате добавления.

Порядок:

- `asc`;
- `desc`.

## 13. Критерии готовности MVP

MVP считается готовым, если:

- сайт запускается локально;
- пользователь может войти по логину и паролю;
- пользователь может зарегистрировать обычный аккаунт;
- пользователь может выйти из аккаунта;
- администратор может создать пользователя;
- администратор может просканировать папку с видео;
- администратор может редактировать метаданные видео;
- администратор может назначить пользователю доступ к конкретным видео;
- обычный пользователь видит только свои видео;
- обычный пользователь не может открыть чужое видео напрямую;
- обычный пользователь не может получить stream чужого видео;
- видео воспроизводится через браузерный плеер;
- работает перемотка;
- большие файлы отдаются через `Range Requests`;
- сортировка по названию работает;
- сортировка по дате работает;
- авторизованный пользователь может загрузить видео через сайт;
- загруженное видео сохраняется чанками и не читается целиком в память;
- после загрузки пользователь попадает на страницу просмотра;
- автор или администратор может удалить видео;
- карточки видео показывают thumbnail/preview и сохраненный прогресс при наведении;
- страница просмотра сохраняет прогресс в БД и восстанавливает позицию;
- история просмотров, `Смотреть позже`, лайки/дизлайки и комментарии работают для каждого аккаунта отдельно;
- профиль пользователя и настройки аккаунта открываются только авторизованному пользователю;
- чужие профили показывают только те видео автора, к которым у текущего пользователя есть доступ;
- миграции БД применяются через Alembic при старте приложения.

## 14. Будущие улучшения

После MVP можно добавить:

- определение длительности через `ffmpeg`;
- PostgreSQL вместо SQLite;
- группы доступа;
- стандартизированные категории видео и фильтрация по ним;
- поиск по названию и описанию;
- поддерживаемый режим доступа с других устройств в локальной сети;
- автоматические тесты для авторизации, доступов, Range-запросов, сортировки, загрузки, профилей и пользовательских сценариев;
- отдельный JSON API для будущего frontend-приложения;
- выбор качества видео;
- субтитры;
- плейлисты;
- подписки;
- уведомления;
- жалобы и расширенную модерацию;
- расширенный поиск и историю поисковых запросов;
- персонализацию рекомендаций;
- бесконечную прокрутку;
- мини-плеер.

## 15. Текущие уточнения продукта

- Главная страница `GET /` доступна без авторизации.
- Неавторизованный пользователь видит заглавную страницу с кнопками входа и регистрации, но не получает доступ к приватным видео.
- Регистрация пользователей доступна через `GET /register` и `POST /register`.
- Для регистрации требуются логин и пароль.
- Новый пользователь создается как обычный пользователь без прав администратора.
- После успешной регистрации создается пользовательская сессия.

## 16. Уточнение текущей главной страницы

- Стартовый экран `GET /` показывает список доступных видеофайлов даже без входа.
- Для гостя карточки видео не открывают просмотр напрямую, а ведут к форме входа.
- Иконка входа находится справа в верхней панели.
- Форма входа может открываться центральной панелью поверх главной страницы.
- Регистрация остается отдельной страницей `/register`.
- Обычный пользователь, зарегистрированный через публичную регистрацию, получает доступ к текущим доступным видео через записи `VideoAccess`.
- Просмотр `/watch/{video_id}` и поток `/videos/{video_id}/stream` остаются защищенными авторизацией и проверкой доступа.

## 17. Будущая функция: доступ с других устройств

- Добавить поддерживаемый режим запуска сервера для подключения с других устройств в локальной сети.
- Для локальной сети сервер должен уметь запускаться с привязкой к `0.0.0.0`.
- Документировать URL вида `http://<local-ip>:8000/` для подключения с другого устройства.
- Документировать требования к Windows Firewall и настройкам сети.
- Доступ из интернета или другой внешней сети не включать без отдельной проработки безопасности, HTTPS, секретов сессии и сетевой схемы.

## 18. Загрузка и удаление пользовательских видео

- Авторизованный пользователь открывает загрузку через пункт `Загрузить видео` в выпадающем меню аккаунта.
- По нажатию на `Загрузить видео` открывается центральная панель загрузки.
- Панель загрузки поддерживает выбор файла и drag and drop.
- Пользователь может указать название видео при загрузке.
- Сервер принимает загрузку через `POST /videos/upload`.
- Сервер сохраняет видео в `video_storage/` чанками, не читая весь файл в память.
- Для файлов, добавленных вручную в `video_storage/`, поддерживаются только расширения из `SUPPORTED_VIDEO_EXTENSIONS`.
- Для пользовательской загрузки поддерживаются входные расширения из `UPLOAD_VIDEO_EXTENSIONS`, а итоговый файл сохраняется как `.mp4`.
- Загруженные H.265/HEVC-видео должны перекодироваться сервером в браузерный `.mp4` с H.264 video, AAC audio, `yuv420p` и `faststart`, если доступен `ffmpeg`.
- Для перекодирования используется `ffmpeg` из `PATH` или путь из переменной окружения `LOCALTUBE_FFMPEG_PATH`.
- Для видео генерируется автоматическое превью из первого кадра через `ffmpeg`.
- Превью хранится как производный JPEG-файл в `video_storage/thumbnails/{video_id}.jpg` и не требует отдельного поля в БД.
- Главная страница показывает превью на карточке видео; если превью еще не создано или `ffmpeg` недоступен, показывается fallback-карточка.
- Доступ к превью идет через `GET /videos/{video_id}/thumbnail`; для авторизованного пользователя применяется та же проверка `VideoAccess`, что и для просмотра видео.
- Пользователь может переключать светлую и темную тему интерфейса; темная тема не должна использовать чистый черный цвет как основной фон.
- Пользователь может скрывать и возвращать боковое меню на главной странице.
- В верхней панели нет отдельной ссылки `Видео`; переход на главную доступен через бренд сайта и пункт `Головна` в боковом меню.
- Основной стек шрифтов интерфейса: `Roboto, Arial, sans-serif`.
- Для каждого аккаунта сохраняется отдельная история просмотров.
- Раздел `История просмотров` доступен в боковом меню главной страницы и может быть очищен пользователем.
- Пункт `Головна` в боковом меню возвращает к общему списку доступных видео.
- Раздел `Смотреть позже` доступен в боковом меню и наполняется кнопкой `Смотреть позже` под видео.
- Раздел `Понравившиеся видео` показывает видео, которым пользователь поставил лайк.
- Лайк и дизлайк являются взаимоисключающими реакциями: повторное нажатие на активную реакцию убирает ее, выбор другой реакции заменяет старую.
- Кнопка `Смотреть позже` на странице просмотра является icon-переключателем: добавляет видео в одноименный список или убирает его оттуда.
- На странице просмотра доступны icon-кнопки лайка, дизлайка и `Смотреть позже`, комментарии и кнопка `Поделиться`.
- Лайк, дизлайк, `Смотреть позже`, добавление и удаление комментариев на странице просмотра должны отправляться на сервер без полной перезагрузки страницы; обычный POST-redirect остается fallback-поведением без JavaScript.
- Комментарии могут удалять автор комментария, администратор и автор видео.
- В блоке других доступных видео на странице плеера отображаются те же thumbnail-превью, что и на главной странице.
- На странице просмотра не показываются отдельные кнопки `Назад`, `Предыдущее` и `Следующее`.
- Кнопки удаления видео показываются только на странице просмотра и не показываются на главной странице.
- После загрузки создается запись `Video`, а автором считается текущий пользователь.
- После загрузки создаются записи `VideoAccess` для всех обычных пользователей, чтобы пользователи видели видео друг друга; администратор видит видео без отдельной записи доступа.
- Для уже существующих обычных пользователей недостающие доступы к пользовательским загрузкам синхронизируются при открытии главной страницы.
- Сервер поддерживает загрузку видео до `10 ГБ` и во время сохранения контролирует размер по записанным чанкам.
- После успешной загрузки пользователь перенаправляется на страницу просмотра загруженного видео.
- Страница просмотра не должна жестко указывать `video/mp4` для всех файлов; браузер должен получать фактический MIME от stream endpoint.
- При отправке формы загрузки пользователь видит анимацию загрузки не менее 1 секунды.
- Удалять видео может только администратор или автор видео.
- Удаление выполняется через `POST /videos/{video_id}/delete`.
- При удалении удаляется файл из `video_storage/` и запись из базы.
- Перед удалением со страницы просмотра frontend должен остановить HTML5-плеер, чтобы освободить файл на Windows.
- При удалении сервер прерывает активные stream-ответы для удаляемого `video_id`.
- Если файл все равно занят системой и не может быть удален физически сразу, сервер скрывает видео из интерфейса через `is_available = false`, удаляет доступы, возвращает пользователя на главную без `500 Internal Server Error` и продолжает удалять файл из `video_storage/` в фоновой retry-задаче.
- После успешного фонового удаления файла сервер удаляет запись `Video` из базы.

## 19. Миграции базы данных

- Схема базы данных управляется через Alembic.
- Текущая версия схемы хранится в таблице `alembic_version`.
- При старте приложения существующая локальная база без `alembic_version` помечается baseline-версией `001_baseline`, затем обновляется до последней версии.
- Новые изменения схемы должны добавляться отдельными файлами в `migrations/versions/`.
- `Base.metadata.create_all()` больше не является основным механизмом обновления схемы.

## 20. Профиль пользователя

- Авторизованный пользователь может открыть страницу `GET /profile`.
- Авторизованный пользователь может открыть профиль другого пользователя через `GET /users/{username}`.
- Неавторизованный пользователь при открытии `/profile` перенаправляется на `/login`.
- Неавторизованный пользователь при открытии `/users/{username}` перенаправляется на `/login`.
- В верхней панели находится аватар пользователя. Он открывает выпадающее меню аккаунта.
- В выпадающем меню аккаунта доступны:
  - профиль;
  - загрузка видео;
  - настройки аккаунта;
  - переключение темы;
  - выход.
- Имя автора на карточке видео и странице просмотра ведет на профиль автора, если автор указан.
- На главной странице карточка видео отображает аватар профиля автора и отображаемое имя, если они заданы.
- На странице просмотра отображается аватар профиля автора и отображаемое имя, если они заданы.
- Если у автора нет аватара, показывается fallback-инициал.
- Если у автора нет отображаемого имени, показывается логин автора.
- На странице профиля пользователь видит:
  - логин;
  - роль;
  - дату регистрации;
  - email, если он указан;
  - отображаемое имя;
  - аватар или fallback-инициал;
  - описание профиля;
  - статистику по доступным видео, истории, списку `Смотреть позже`, лайкам и сохраненному прогрессу.
- В профиле пользователя отображаются загруженные автором видео.
- Для обычного пользователя в чужом профиле показываются только те видео автора, к которым у текущего пользователя есть доступ через серверную проверку.
- Администратор в чужом профиле видит все доступные видео автора.
- Если текущий пользователь открывает `/users/{username}` для самого себя, он перенаправляется на `/profile`.
- Страница `GET /profile` не содержит формы редактирования аккаунта.
- Настройки аккаунта доступны на странице `GET /account`.
- Пользователь может изменить через `POST /account`:
  - отображаемое имя;
  - email;
  - описание профиля;
  - аватар.
- Пользователь может изменить пароль через `POST /account/password`.
- Старые маршруты `POST /profile` и `POST /profile/password` сохраняются как совместимые алиасы и перенаправляют пользователя в настройки аккаунта.
- Для смены пароля требуется текущий пароль.
- Новый пароль и подтверждение должны совпадать.
- Аватары хранятся в `app/static/uploads/avatars/` и не коммитятся в git.
- Поддерживаемые форматы аватаров: `.jpg`, `.jpeg`, `.png`, `.webp`.
- Максимальный размер аватара: `2 МБ`.

Новые элементы схемы:

- `users.email`;
- `user_profiles`.
